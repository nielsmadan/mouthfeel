# tally

Small summary-statistics helpers: `mean`, `median`, `percentile` (nearest-rank), and a
`formatSummary` one-liner. No dependencies.

Run the tests with `npm test` (node's built-in test runner).
