{
  "name": "mouthfeel",
  "version": "0.1.0",
  "description": "Temporary output styles for coding agents",
  "type": "module",
  "license": "MIT",
  "author": "Niels Madan",
  "repository": "https://github.com/nielsmadan/mouthfeel",
  "keywords": [
    "coding-agent",
    "output-style",
    "persona",
    "tone"
  ],
  "private": true,
  "engines": {
    "node": ">=22.19.0"
  },
  "scripts": {
    "setup": "npm ci && lefthook install && npm run doctor",
    "doctor": "test -d node_modules && lefthook validate && test -x \"$(git rev-parse --git-path hooks/pre-commit)\" && test -x \"$(git rev-parse --git-path hooks/pre-push)\" || { echo 'Setup incomplete; run npm run setup' >&2; exit 1; }",
    "build": "tsx scripts/build.ts",
    "check": "python3 -B -m unittest discover -s scripts -p 'test_release*.py' && tsc --noEmit && npm test",
    "type-check": "tsc --noEmit",
    "check-all": "npm run type-check",
    "dev:claude": "tsx scripts/dev-claude.ts",
    "dev:codex": "tsx scripts/dev-codex.ts",
    "dev:oc": "tsx scripts/dev-opencode.ts",
    "dev:pi": "tsx scripts/dev-pi.ts",
    "eval:baseline": "tsx harness/baseline.ts",
    "eval:hosts": "tsx harness/run.ts",
    "eval:review": "tsx harness/review-page.ts",
    "eval:serve": "tsx harness/serve.ts",
    "eval:prepare": "tsx scripts/prepare-evals.ts",
    "pretest": "npm run build",
    "test": "tsx --test \"tests/**/*.test.ts\"",
    "release": "python3 scripts/release.py"
  },
  "devDependencies": {
    "@earendil-works/pi-coding-agent": "^0.84.2",
    "@opencode-ai/plugin": "^1.18.18",
    "@types/node": "^24.0.0",
    "esbuild": "^0.25.0",
    "lefthook": "^2.1.14",
    "tsx": "^4.20.0",
    "typescript": "^5.9.0",
    "yaml": "^2.8.0"
  }
}
